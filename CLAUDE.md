# Dotfiles Notes

## Bare Git Repo
The dotfiles repo is a bare git repo at `~/.dotfiles`.
Use: `git --git-dir=$HOME/.dotfiles --work-tree=$HOME <command>`

Or use the alias (after sourcing .zshrc): `dotfiles <command>`

## Preferences
- Don't add "Co-Authored-By: Claude" to commits
