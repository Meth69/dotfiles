[mgr]
show_hidden = true

[opener]
edit = [{ run = 'helix "$@"', block = true, desc = "Helix" }]
audio = [{ run = 'vlc "$@"', orphan = true, desc = "VLC" }]

[open]
text = "edit"

[[open.rules]]
mime = "audio/*"
use = "audio"
